#include <stdio.h>
/**
 * mul - mutiplies two intengers
 * @a: one of the intengers to be multiplied
 * @b: one of the intengers being multiplied
 * Return: result of multiplication of the parameters
 */

int mul(int a, int b)
{
	return (a * b);
}
