#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 * @lavie: first number input
 * @brian: second number input
 * Return: lavie + brian
 */

int add(int lavie, int brian)
{
	return (lavie + brian);
}
